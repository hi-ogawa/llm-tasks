[project]
name = "ableton-manual-scraper"
version = "0.1.0"
description = "Scrape Ableton Live 12 manual and convert to LLM context"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "html2text>=2025.4.15",
    "trafilatura>=2.0.0",
]
